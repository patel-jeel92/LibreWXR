# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environment
.venv/

# Environment (contains local config)
.env

# Per-host compose overrides (memory caps, bind mounts, tunnel credentials, etc.)
docker-compose.override.yml

# IDE
.vscode/
.idea/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Duplicate data file (canonical copy in src/librewrx/colors/)
/color_table.csv

# Persistent satellite grid cache (local dev)
cache/

# Claude Code
.claude/
.claude-context/

# Per-host opt-in sentinel for scripts/auto-update.sh
.auto-update-enabled

# Site (managed separately, not part of the API repo)
librewxr-site/

# Outgoing correspondence drafts (kept local, sent manually)
emails/

# OpenCode example (not ready for release)
examples/opencode-example.html
